name: tefa_parkir
description: "A new Flutter project."
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter
  qr_flutter: ^4.1.0

  intl: ^0.19.0
  cupertino_icons: ^1.0.8
  supabase_flutter: ^2.9.1
  permission_handler: 11.3.1
  mobile_scanner: 5.1.1   # ✅ versi lama, masih ada boundingBox
  camera: ^0.11.0+2
  image_picker: 1.2.0
  uuid: 4.5.1
  font_awesome_flutter: 10.7.0
  google_fonts: ^6.3.1
  google_mlkit_text_recognition: ^0.15.0


dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/sounds/beep.mp3
